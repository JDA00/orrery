# Textures are fetched from GitHub Releases by `./gradlew fetchTextures`
# (invoked automatically by `./gradlew run`). This file keeps the
# directory in git. See gradle.properties and build.gradle.kts.